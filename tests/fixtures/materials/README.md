# materials — 教材形態樣本

同一段程式碼在不同形態的教材上表現差異極大。這裡的三份樣本各自隔離一個變因，
用途是**讓啟發式規則（條目估算、切段）在各形態上都有回歸測試**，
而不是只對日文詞條頁有效。

判準與各形態捕捉到的問題見上層 [README.md](../README.md)〈教材形態〉。

實際的模型呼叫測試不放這裡（需要本地模型，屬 `manual`）；
本目錄的樣本主要供 `tests/stages/test_extract.py` 的純邏輯測試使用。
